<!--
 * @Description: 
 * @Author: 薛山山
 * @Date: 2026-04-30 10:42:23
 * @LastEditors: 薛山山
 * @LastEditTime: 2026-04-30 10:42:24
-->
# 回归测试用例（每次功能改动后必跑）

> 目标：确保跨视图导航、搜索、跳转、上下文清理在改动后保持可用。
> 环境：本地 HTTP 服务（例如 `python3 -m http.server 8888`），浏览器访问 `http://localhost:8888`。

## TC-01 页面基础加载

- 步骤：打开首页，等待加载完成。
- 期望：页面无白屏；顶部导航、主内容区、侧边栏可见；Console 无报错。

## TC-02 五个视图切换

- 步骤：依次点击“关系图谱 / 家族谱系 / 人物名录 / 回目视图 / 知识库”。
- 期望：每个视图都能正常渲染；切换后仅当前视图面板可见；无卡死。

## TC-03 图谱搜索（侧栏搜索）

- 步骤：在图谱侧栏搜索输入“宝玉”，点击一条结果。
- 期望：结果列表正确出现；点击后进入对应人物上下文；“清空上下文”可用。

## TC-04 树视图与名录搜索

- 步骤：切换到家族谱系，搜索“宝玉”；再切换到人物名录，搜索“黛玉”。
- 期望：两处搜索都能得到有效过滤结果，列表数量或内容发生预期变化。

## TC-05 视图内跳转

- 步骤：在人物卡中触发“查看关系”或“知识相关”入口；再使用返回按钮。
- 期望：能跳到目标上下文；返回可回到上一个可理解状态；不会丢失当前视图。

## TC-06 清空上下文

- 步骤：先选中任意人物或专题，再点击“清空上下文”。
- 期望：选中状态被清理；弹层关闭；图谱视图下回到重要人物概览；无异常提示。

## TC-07 关系对比

- 步骤：在“人物对比”选择两个人物并点击“查看关系”。
- 期望：关系视图/抽屉内容正确展示；二者关系网络可见；可继续进行其他操作。

## TC-08 移动端宽度回归（可选但建议）

- 步骤：将视口调整为约 390x844 后重复 TC-02 与 TC-03。
- 期望：视图可用；搜索可交互；无关键按钮遮挡。

## TC-09 知识库切换滚动稳定性

- 步骤：先在任一视图将页面向下滚动（例如 `window.scrollTo(0, 600)`），然后反复执行“关系图谱 -> 知识库 -> 关系图谱 -> 知识库”至少 3 轮。
- 期望：每次切换到知识库时，页面滚动位置不出现“自动下掉一点”的累积漂移（`window.scrollY` 仅允许极小浮动，建议阈值不超过 2px）。

## 场景链路用例（建议涉及联动改动时必跑）

## TC-10 URL 参数直达 + 人物上下文恢复

- 场景：验证深链参数在非图谱视图下也能正确恢复人物上下文。
- 步骤：直接打开 `/?view=knowledge&char=lin_daiyu`。
- 期望：默认激活“知识库”视图；全局上下文显示“知识库 / 林黛玉”；人物详情卡片可见；知识库仍保持自身显式筛选状态，不应被偷偷改成家族筛选或其他隐藏条件。

## TC-11 图谱人物卡 -> 知识库人物检索

- 场景：验证“人物卡里的相关知识”是一次显式带参跳转，而不是仅做高亮。
- 步骤：在关系图谱搜索“宝玉”，打开人物卡，点击“相关知识条目”中的任一入口。
- 期望：切换到“知识库”视图；搜索框自动填入对应人物名；首个匹配知识条目被定位或高亮；相关人物高亮仍保留。

## TC-12 人物名录 -> 知识库显式带参跳转

- 场景：验证名录中的“相关知识/知识”按钮会把当前人物作为查询参数带入知识库。
- 步骤：切换到“人物名录”，找到“林黛玉”或“贾宝玉”，点击卡片上的“相关知识”或“知识”按钮。
- 期望：进入“知识库”视图；搜索框为该人物姓名；知识条目列表与该人物相关；全局上下文仍能看出当前人物是谁。

## TC-13 回目视图 -> 知识库按章回跳转

- 场景：验证回目视图的“在知识库查看本回”会显式携带章回参数。
- 步骤：切换到“回目视图”，任选一回，点击“在知识库查看本回”或“打开完整知识库”。
- 期望：切换到“知识库”视图；章回筛选值为当前回次；排序方式切为按章回；列表内容与该回相关，不应丢到全库默认状态。

## TC-14 回目标签/知识条目 -> 知识库章回 + 关键词联动

- 场景：验证从回目里的标签或具体知识卡跳转时，会同时带上章回与查询词。
- 步骤：在“回目视图”点击某个知识点标签，或点击某张知识卡上的“去知识库查看”。
- 期望：进入“知识库”视图；章回筛选保持为当前回；搜索框自动填入被点击的标签词或知识标题；首屏结果应能解释这次跳转的来源。

## TC-15 单人物上下文跨视图高亮隔离

- 场景：验证“点击人物后再切别的视图”只传播可见高亮，不注入隐式过滤。
- 步骤：先在任一视图点击“王熙凤”进入人物上下文；随后依次切到“家族谱系 / 人物名录 / 知识库”。
- 期望：顶部上下文持续显示当前人物；家族谱系只高亮相关人物，不应自动切家族；人物名录可滚动到该人物或高亮该人物，但 family/gender/sort 不应被悄悄改值；知识库只做相关人物高亮，不应无提示地改主类别、子类或章回。

## TC-16 知识库内点击人物 -> 继续切视图

- 场景：验证知识库中的人物入口先建立人物上下文，再带到其他视图继续探索。
- 步骤：在“知识库”任意知识卡点击关联人物；确认人物卡弹出后，再切到“关系图谱”与“人物名录”。
- 期望：人物卡能正常打开；切到“关系图谱”后仍围绕该人物进行关系探索；切到“人物名录”后该人物应可被快速定位或高亮；整个过程不应丢失当前人物上下文。

## TC-17 参数化跳转后的清空上下文

- 场景：验证“清空上下文”清的是选择态，不是误伤当前视图里用户显式操作出来的查询条件。
- 步骤：先执行 TC-11 或 TC-13 任一带参跳转，再点击顶部“清空上下文”。
- 期望：人物选中态、关系态、弹层关闭；全局上下文恢复成仅显示当前视图；若当前处于知识库/回目/名录等非图谱视图，应停留在当前视图；图谱视图下才回到重要人物概览；显式可见的本地查询条件是否保留，应与当前产品约定一致且在 UI 中可解释。

## TC-18 关系对比后的跨视图回退

- 场景：验证“人物对比/关系链”不会污染其他视图的本地筛选，并且可以安全清理。
- 步骤：在侧边栏选择“贾宝玉”和“林黛玉”，点击“查看关系”或“查找路径”；随后切到“家族谱系”或“知识库”，最后点击“清空上下文”。
- 期望：图谱中两人关系网络或路径正常出现；切到其他视图后仅保留可见上下文提示与相关高亮，不应把关系视图里的隐式图谱过滤带入树/知识库；清空后可回到当前视图的中性状态并继续操作。

## TC-19 左侧工具栏宽度与知识库高频标签展示

- 场景：验证左侧工具栏默认不会过窄，知识库高频标签区不会像被折叠一样只露出少量内容。
- 步骤：打开首页后切到“关系图谱”和“知识库”，观察左侧栏宽度与知识库高频标签区域。
- 期望：图谱左侧栏宽度足够显示常用控件；知识库高频标签区完整展开，必要时可滚动，但不应呈现成一个很窄的小折叠块。

## TC-20 人物名录点击后列表不抖动

- 场景：验证点击人物卡片只更新高亮，不触发整列表重绘导致的跳动。
- 步骤：切换到“人物名录”，点击任意人物卡片，再观察列表区域与当前卡片位置。
- 期望：列表滚动位置保持稳定；点击后只有选中态或相关高亮变化，不应出现明显的整体抖动或重排闪动。

## TC-21 回目视图圆点与提示框边界稳定

- 场景：验证时间线/回目视图中的圆点不会被挤变形，提示框不会跑出浏览器窗口。
- 步骤：切换到“回目视图”或时间线视图，悬停/点击章回圆点，尤其测试靠近底部和右侧的节点。
- 期望：圆点保持正圆；提示框在视口内可见，靠近边缘时会自动翻转或钳制，不会超出窗口。

## TC-22 人物回目数据补全回归

- 场景：验证人物章节数据不会只保留少量显然不完整的回目数。
- 步骤：在人物详情、时间线或数据导出逻辑中检查“史湘云”等人物的回目分布。
- 期望：人物回目数据应包含原始角色相关知识条目所能推导出的回目，数量明显不应只有极少数；若存在不确定项，应标注待校验而不是空缺。

## TC-23 滚动容器归属回归（防双滚动条）

- 场景：验证页面不会同时出现“窗口滚动 + 视图内滚动”相互打架，且各视图滚轮行为一致。
- 步骤：依次进入“人物名录 / 家族谱系 / 回目视图 / 知识库 / 时间线”，在主内容区连续滚动；同时观察浏览器窗口滚动位置是否变化。
- 期望：窗口本身不应成为主滚动层；每个视图应在自己的主内容区滚动；不应出现两个滚动条都在动或必须先滚外层再滚内层的情况。

## TC-24 关系路径面板隐藏态回归

- 场景：验证底部关系路径面板在未激活时不会露出一截。
- 步骤：首次进入页面，不触发“查找路径”；观察底部是否有路径面板边角露出。随后触发一次“查找路径”并关闭面板。
- 期望：未激活时路径面板完全不可见；激活后正常滑入；关闭后彻底隐藏，不残留边框或阴影。

## TC-25 图谱侧栏拖拽缩放生效（sidebar-resize-handle）

- 场景：验证图谱侧栏拖拽缩小后，侧栏本体宽度会真实变小，而不是仅变量变化。
- 步骤：进入“关系图谱”，记录侧栏初始宽度；拖动 `sidebar-resize-handle` 向左至少 120px；再次记录侧栏宽度。
- 期望：`--sidebar-width` 与侧栏实际可视宽度同步变小；侧栏不应被固定 `min-width` 卡死；主要控件仍可见且可滚动。

## TC-26 时间线与知识库顶部可见性回归（防首屏被盖住）

- 场景：验证切换到“时间线”“知识库”后，顶部工具栏/英雄区不会被 header 或全局上下文条遮挡。
- 步骤：先进入任一人物上下文（确保全局上下文条显示），再切到“时间线”和“知识库”。
- 期望：
  - 时间线：`.tl-toolbar.card-surface` 完整可见，顶部不被裁切，只显示一半视为失败。
  - 知识库：`.knowledge-hero.card-surface` 完整可见，首屏标题与统计卡顶部不被遮挡。

## TC-27 全局视图首屏裁切排查（通用）

- 场景：防止后续改动再次出现“首屏卡片/工具栏顶部被盖住”类问题。
- 步骤：在“家族谱系 / 人物名录 / 回目视图 / 知识库 / 时间线”逐一切换，并保持全局上下文条可见。
- 期望：每个视图首屏第一个主区块顶部完全可见；不存在被 header、context bar 或容器 `overflow` 裁掉上边缘的情况。

## TC-28 人物卡时间轴 Hover 详情

- 场景：验证右侧人物抽屉中的出场时间轴具备可探索反馈，不是静态条。
- 步骤：在任意视图打开人物卡（例如贾宝玉），将鼠标移入 `card-fate-timeline-wrap` 中任一高亮节点（active cell）。
- 期望：详情区即时显示“第X回 + 回目/摘要”信息；移出后恢复默认提示；键盘 focus 也可触发同样反馈。

## TC-29 图谱章回时间线模式切换（单回/累计）

- 场景：验证图谱时间线在“单回”与“累计”两种模式下都有清晰反馈与可解释差异。
- 步骤：切到关系图谱，将时间线滑块拖到第20回，先选“单回”再选“累计”。
- 期望：
  - `timeline-chapter` 文案包含“单回/累计”模式标识与人物数。
  - 同一章回下，两种模式显示的人物规模存在明显差异（累计应不小于单回）。

## TC-30 图谱时间线播放速度与滑块联动

- 场景：验证播放按钮、速度选择与滑块进度值之间联动可感知。
- 步骤：在图谱时间线选择“快速”，点击播放，观察约1秒后滑块数值变化，再点击暂停。
- 期望：播放期间滑块值持续递增、章回标签同步更新；暂停后停止增长；不会出现“按钮在播但进度不动”。

## TC-31 图谱时间线拖动反馈（防无响应）

- 场景：验证用户手动拖动滑块时，图谱内容与上下文立即响应。
- 步骤：在图谱视图连续拖动时间线滑块到多个不同章回（如 5 / 20 / 55）。
- 期望：每次拖动后 `timeline-chapter` 与图谱上下文文案同步变化，节点集合有可观察变化；不应出现长时间无反馈。

## TC-32 图谱可用性与干扰控制

- 场景：验证图谱在默认状态下不应出现过度视觉噪声，且入口提示清晰。
- 步骤：进入图谱后观察提示区、粒子动画开关、时间线控件；切换“粒子动画”开关前后比较。
- 期望：默认提示文案可读、操作入口可见；关闭粒子后连线不再出现频繁闪烁干扰；开启后可恢复且不影响基础操作。

## 执行记录模板

- 日期：
- 改动范围：
- 通过：TC-01, TC-02, TC-03, TC-04, TC-05, TC-06, TC-07, TC-08, TC-09, TC-10, TC-11, TC-12, TC-13, TC-14, TC-15, TC-16, TC-17, TC-18, TC-19, TC-20, TC-21, TC-22, TC-23, TC-24, TC-25, TC-26, TC-27, TC-28, TC-29, TC-30, TC-31, TC-32
- 失败项与修复：
